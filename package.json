{
  "name": "tut-rest",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "packageManager": "npm@11.12.1",
  "scripts": {
    "dev": "turbo dev",
    "build:types": "turbo run build:types",
    "db:seed": "npm run db:seed -w server",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.10.0"
  }
}
